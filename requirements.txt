# ============================================================================
# Umbrella requirements — install the FULL stack:
#     uv pip install -r requirements.txt
# For inference-only local use, install requirements-local.txt instead.
# See requirements-train.txt (Colab/GPU) and requirements-local.txt (CPU).
# ============================================================================
-r requirements-train.txt
-r requirements-local.txt
